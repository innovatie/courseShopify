{% comment %} {{ 'section-faq.css' | asset_url | stylesheet_tag }} {% endcomment %}

<div class="page-width faq container py-4">
  <h2 class="h1">{{ section.settings.headline }}</h2>

  {% for block in section.blocks %}
    <div class="faq__row">
      <input type="checkbox" id="question-{{ forloop.index }}" class="faq__toggle">
      <label for="question-{{ forloop.index }}" class="faq__question">{{ block.settings.question }}</label>
      <div class="faq__answer">{{ block.settings.answer }}</div>
    </div>
  {% endfor %}
</div>

{% schema %}
{
  "name": "FAQ",
  "settings": [
    {
      "type": "text",
      "label": "Headline",
      "id": "headline"
    }
  ],
  "blocks": [
    {
      "name": "FAQ Question",
      "type": "question",
      "settings": [
        {
          "type": "text",
          "label": "Question",
          "id": "question"
        },
        {
          "type": "text",
          "label": "Answer",
          "id": "answer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FAQ"
    }
  ]
}
{% endschema %}
